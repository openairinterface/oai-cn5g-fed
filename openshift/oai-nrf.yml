# SPDX-License-Identifier: MIT

#---------------------------------------------------------------------
#
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
   name: oai-nrf
spec:
  lookupPolicy:
    local: true
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: oai-nrf
spec:
    runPolicy: "Serial"
    source:
        git:
            uri: "https://github.com/openairinterface/oai-cn5g-nrf.git"
            ref: "develop"
    strategy:
        dockerStrategy:
            dockerfilePath: "docker/Dockerfile.nrf.centos"
    output:
        to:
            kind: "ImageStreamTag"
            name: "oai-nrf:develop"
