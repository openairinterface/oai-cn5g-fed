# SPDX-License-Identifier: MIT

#---------------------------------------------------------------------
#
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
   name: oai-ausf
spec:
  lookupPolicy:
    local: true
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: "oai-ausf"
spec:
    runPolicy: "Serial"
    source:
        git:
            uri: "https://github.com/openairinterface/oai-cn5g-ausf.git"
            ref: "develop"
    strategy:
        dockerStrategy:
            dockerfilePath: "docker/Dockerfile.ausf.centos"
    output:
        to:
            kind: "ImageStreamTag"
            name: "oai-ausf:develop"
