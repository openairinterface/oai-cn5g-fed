# SPDX-License-Identifier: MIT

#---------------------------------------------------------------------
#
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
   name: oai-upf
spec:
  lookupPolicy:
    local: true
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: "oai-upf"
spec:
    runPolicy: "Serial"
    source:
        git:
            uri: "https://github.com/openairinterface/oai-cn5g-upf.git"
            ref: "develop"
    strategy:
        dockerStrategy:
            dockerfilePath: "docker/Dockerfile.upf.centos"
    output:
        to:
            kind: "ImageStreamTag"
            name: "oai-upf:develop"
